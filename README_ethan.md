# Sound Sense — Ethan's Branch

This branch contains the frontend/UI and model-integration work for Sound Sense, a speech emotion recognition product, plus EDA and data-cleaning contributions covering MFCC and acoustic feature analysis on the merged emotion dataset.

## Folder / file map

```
.
├── README_ethan.md          This file
├── README.md                 Repo root readme (project name only)
├── ethan_research.md         Research notes: what MFCCs are and why they're the
│                              standard feature extraction method for audio classification
│
├── EDA/
│   └── ethan_eda.ipynb        Dataset inventory, SAVEE folder-label audit, sample-rate
│                              audit, per-emotion feature exploration (waveform, mel-spec,
│                              MFCCs, RMS, pitch, centroid, ZCR, chroma), and an ANOVA
│                              feature-discrimination ranking
│
├── cleaning/
│   └── ethan_cleaning.ipynb   Turns the merged dataset into a model-ready manifest: drops
│                              the SAVEE source (mislabeled by speaker, not emotion), drops
│                              the Surprised class (severe imbalance, ~4.6% of the data),
│                              quarantines corrupted/too-short/too-long files, and writes
│                              the cleaned manifest + summary CSVs
│
├── outputs/                   CSV outputs from the cleaning notebook (cleaned manifest,
│                              corrupted/quarantined file lists, EDA summary)
│
├── plots/                     Figures generated by the EDA notebook (class distribution,
│                              waveforms, spectrograms, MFCCs, ANOVA ranking, etc.)
│
├── frontend/                  React + Vite web app — the Sound Sense demo UI
│   ├── src/
│   │   ├── App.jsx             Top-level component: owns the idle/recording/analyzing/
│   │   │                      result state machine and lays out the three cards
│   │   ├── index.css           Design tokens, gradient background, card/tile/animation styles
│   │   ├── components/
│   │   │   ├── EmotionTile.jsx   One emotion cell: emoji, label, confidence bar, rank ring
│   │   │   ├── MicButton.jsx     Circular glass mic button with recording pulse animation
│   │   │   ├── ProgressBar.jsx   5-second listening progress bar with a live counter
│   │   │   └── ResultsPanel.jsx  Top-3 ranked results with an empty state
│   │   ├── hooks/
│   │   │   └── useRecorder.js    Owns MediaRecorder: permission, 5s auto-stop, cleanup
│   │   └── lib/
│   │       ├── emotions.js       The 6 emotion definitions (key, label, emoji) + ranking
│   │       └── predictEmotion.js Single integration point with the backend (see below)
│   └── package.json
│
└── backend/
    ├── emotion_app.py          FastAPI server serving the WavLM-large emotion model
    └── w2v2_ser_best/           Model weights + config (gitignored, see Setup below)
```

## Frontend

The `frontend/` app is a React 19 + Vite single-page UI, styled with a "Calm Clarity" design language: a soft indigo gradient background (`#F5F7FF → #C9CFF7 → #7C82E8`), frosted glass cards, and a restrained deep-indigo/green accent palette rather than saturated colors or heavy motion.

The UI is a single state machine with four states, driven from `App.jsx`:

1. **Idle** — mic button ready, all six emotion tiles at 0%, results panel shows an empty state.
2. **Recording** — mic button pulses, the progress bar fills over a 5-second window with a live counter, capped by `useRecorder`'s auto-stop (or an early second tap). The window matches the model's training data: it was trained on 1–5 second clips and the backend does not trim input, so longer recordings are out-of-distribution.
3. **Analyzing** — emotion tiles show a loading shimmer while the recorded clip is sent to the backend.
4. **Result** — tiles animate to their confidence values, the top 3 get a graded ring (full/partial/faint), and the results panel lists the top 3 ranked emotions.

If microphone permission is denied, or the backend is unreachable, an inline error message replaces the status line instead of the UI silently hanging.

### Running the frontend

```bash
cd frontend
npm install
npm run dev
```

Open `http://localhost:5173`.

## Backend integration

The UI does not run any model itself — it talks to a separate WavLM-large inference server over HTTP. All of that integration is isolated in one file, `src/lib/predictEmotion.js`, so the rest of the app only ever calls `predictEmotion(audioBlob)` and gets back a plain object of percentages.

What that function does:

1. **Re-encodes the audio.** `MediaRecorder` produces `audio/webm;codecs=opus`, but the backend decodes uploads with `soundfile`, which cannot read webm. Before uploading, the blob is decoded with the Web Audio API, resampled to 16 kHz, mixed down to mono, and re-encoded as a 16-bit PCM WAV — mirroring the encoding the backend's own built-in test page uses.
2. **Uploads it.** `POST http://localhost:7860/predict` as multipart form data, field name `audio`, filename `clip.wav`.
3. **Transforms the response.** The backend replies with fractional probabilities and Capitalized class names, e.g. `{"probabilities": {"Angry": 0.6123, "Happy": 0.031, ...}}`. This is converted into lowercase integer percentages (`{angry: 61, happy: 3, ...}`) matching what the tile and results components expect.
4. **Fails gracefully.** If the fetch throws (server not running) or returns a non-OK status, `predictEmotion` throws an `Error` with a plain-language message that `App.jsx` surfaces inline, rather than leaving the UI stuck on "Analyzing…".

The model classifies **6 emotions** — Angry, Disgusted, Fearful, Happy, Neutral, Sad — matching the classes retained in this branch's cleaning notebook (Surprised was dropped from training due to severe class imbalance, and the frontend grid only ever renders these 6).

Model weights live in `backend/w2v2_ser_best/` but are excluded from git via `.gitignore` because of their size. To run the backend locally, place the trained model files (`config.json`, `preprocessor_config.json`, and the weights file — `model.safetensors` or `pytorch_model.bin`) directly inside `backend/w2v2_ser_best/`.

## Setup (backend)

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install torch transformers librosa soundfile fastapi uvicorn python-multipart
python emotion_app.py
```

The server starts on `http://localhost:7860` and picks CUDA, then MPS, then CPU automatically. With the frontend dev server also running, recording in the browser sends real audio to this endpoint and the UI renders the model's actual predictions.

## Credit

`backend/emotion_app.py` and the WavLM-large model — written and trained by **Haojie Huang**.

Frontend (`frontend/`) and backend integration (`src/lib/predictEmotion.js`) — **Ethan Perez**.
