# Copy this file to `.env` and fill in the real values (get them from the Aiven console).
# `.env` is gitignored — never commit real credentials. Share the password privately, not in the repo.
MYSQL_HOST=your-service.aivencloud.com
MYSQL_PORT=12345
MYSQL_USER=avnadmin
MYSQL_PASSWORD=changeme
MYSQL_DB=defaultdb
