# Large audio emotion dataset (~1.8 GB) — keep it out of git.
Emotions/
Emotions_processed/
Emotions_w2v2_16k/

# Model weights — download from HF Hub or Colab
Notebooks/w2v2_ser_best/
*.keras
*.pt
*.npz

# Local SQLite db built by SQLCLEAN.ipynb — small, but reproducible from ../Emotions in a couple
# of minutes, so each teammate builds their own instead of committing a binary file.
Notebooks/ser_local.db

# macOS junk
.DS_Store

# Local secrets (never commit)
.env

# Claude internal
.claude/
